<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Update</title>
  </head>
  <body class="">
    <div class="flex flex-col gap-10 items-center justify-center h-screen">
      <h1 class="text-3xl font-bold">Update User</h1>
      <form
        action="/update/<%= user.id %>"
        method="POST"
        class="flex flex-col gap-3 w-[300px] md:w-[400px] max-w-[600px]"
      >
        <p>Name:</p>
        <input
          type="text"
          name="name"
          value="<%= user.name %>"
          class="border p-2 rounded w-full"
        />
        <p>Age:</p>
        <input
          type="number"
          name="age"
          value="<%= user.age %>"
          class="border p-2 rounded w-full"
        />
        <div class="flex justify-between">
          <a href="/" class="bg-gray-500 text-white px-3 py-1 rounded">
            Cancel
          </a>
          <button
            type="submit"
            class="bg-blue-500 text-white px-3 py-1 rounded"
          >
            Update
          </button>
        </div>
      </form>
    </div>
  </body>
</html>
